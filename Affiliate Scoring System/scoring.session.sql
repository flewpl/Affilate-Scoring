CREATE DATABASE scoring;
USE scoring;

--  # connection for log in to database
-- connection = mysql.connect(
--     host='127.0.0.1',
--     user='flewpl',
--     password='root',
--     database='scoring'
-- )